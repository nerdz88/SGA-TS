html
  head
    title= title
    link(rel='stylesheet' href='https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css')
    //- link(rel='stylesheet' href='/css/style.css')
  body
    =JSON.stringify(cours)
    div.container
      h1 #{cours.getSigle()} - #{cours.getTitre()} - Gr: #{cours.getGroupeCours()[0].getNumero()} 
      ul.entries 
        li Nb étudiant Max: #{cours.getGroupeCours()[0].getNbMaxEtudiant()} 
        li Date début: #{cours.getDateDebut()}
        li Date fin: #{cours.getDateFin()}

      h1 Liste des étudiants  
      ul.entries
        each unEtudiant in cours.getGroupeCours()[0].getEtudiants()
          li         
            strong #{unEtudiant.getNom()} #{unEtudiant.getPrenom()} 
            | : #{unEtudiant.getEmail()} - #{unEtudiant.getCodePermanent()}
        else
          li
            em Pas d'étudiants dans le cours

<script src="//code.jquery.com/jquery-2.2.4.min.js"></script>
<script src="//stackpath.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js"></script>
<script type="text/javascript" src="/lib/enseignant/liste-cours.js"></script>